# lang_experiment

This repository contains simple NLP experiments. The `sentiment_app.py` script
provides a small command line utility for calculating a sentiment score for text
input. The score ranges from -1 (negative) to 1 (positive).

## Running the sentiment app

```bash
python3 sentiment_app.py
```
Enter your text at the prompt and the script will output a sentiment score.
